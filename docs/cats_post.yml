summary: Create one cat
tags:
- cats
parameters:
- name: Cat
  in: body
  schema:
    type: object
    required:
    - name
    - age
    properties:
      name:
        type: string
        maxLength: 31
        description: naming cat
      age:
        type: integer
responses:
  "201":
    description: OK
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/Cat'
